<Window x:Class="ChatClient.ChangeUsernameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="ChangeUsernameDialog" Height="300" Width="400">
    <Grid>
        <StackPanel Margin="24">
            <TextBlock FontSize="24">사용자 이름 변경</TextBlock>
            <Grid Margin="24"></Grid>
            <TextBlock FontSize="18">새 사용자 이름</TextBlock>
            <TextBox x:Name="NewUsernameBox"></TextBox>
        </StackPanel>
        <Button Click="ChangeBtnClick" HorizontalAlignment="Right" VerticalAlignment="Bottom" IsDefault="True" Margin="24">변경</Button>       
    </Grid>
</Window>
